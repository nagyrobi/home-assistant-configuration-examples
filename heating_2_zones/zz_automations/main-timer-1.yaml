##### Automations for heating timer Zone 1
##### Workdays

- id: heating1_morning_on_wd
  alias: Heating zone 1 set temp (weekday mornings)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekday_morning_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'on'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_morning') }}"

- id: heating1_daytime_on_wd
  alias: Heating zone 1 set temp (weekday daytime)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekday_day_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'on'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_day') }}"

- id: heating1_evening_on_wd
  alias: Heating zone 1 set temp (weekday evenings)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekday_evening_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'on'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_evening') }}"

- id: heating1_nighttime_on_wd
  alias: Heating zone 1 set temp (weekday nightime)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekday_night_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'on'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_night') }}"



##### Weekends

- id: heating1_morning_on_we
  alias: Heating zone 1 set temp (weekend mornings)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekend_morning_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'off'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_morning') }}"

- id: heating1_daytime_on_we
  alias: Heating zone 1 set temp (weekend daytime)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekend_day_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'off'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_day') }}"

- id: heating1_evening_on_we
  alias: Heating zone 1 set temp (weekend evenings)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekend_evening_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'off'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_evening') }}"

- id: heating1_nighttime_on_we
  alias: Heating zone 1 set temp (weekend nightime)
  trigger:
    platform: template
    value_template: "{{ states('sensor.time') == (state_attr('input_datetime.heating_weekend_night_start','timestamp') | timestamp_custom('%H:%M', False)) }}"
  condition:
    condition: and
    conditions:
    - condition: state
      entity_id: 'binary_sensor.workday_sensor'
      state: 'off'
    - condition: state
      entity_id: 'input_boolean.heating_timer_switch'
      state: 'on'
    - condition: template
      value_template: "{{ not is_state_attr('climate.heater_1', 'preset_mode', 'away') }}"
  action:
  - service: climate.set_temperature
    data_template:
      entity_id: climate.heater_1
      temperature: "{{ states('input_number.heating_temp1_night') }}"

